'use strict';

const cacheManagement = {
  // Cache enum types Start //

  memcached: 'memcached',

  in_memory: 'in_memory',

  shared_memcached: 'shared_memcached'

  // Cache enum types Start //
};

module.exports = cacheManagement;
