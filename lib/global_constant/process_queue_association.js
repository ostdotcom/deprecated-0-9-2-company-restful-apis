'use strict';

const processQueueAssociation = {
  // Status enum types Start //

  availableForAllocations: 'available',

  dedicated: 'dedicated',

  full: 'full',

  processKilled: 'killed'

  // Status enum types end //
};

module.exports = processQueueAssociation;
